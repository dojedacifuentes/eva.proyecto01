# Publicación de EVA v2 — 19 de septiembre de 2026

La rama original `landing-eva-v2` se subió y se integró por fast-forward en `main`.
Vercel desplegó el commit `53db65c`; se verificó HTTP 200 y el contenido v2 en
https://evaproyecto01.vercel.app/. Canonical y Open Graph usan ese dominio.
La versión anterior está preservada en el tag `backup-pre-landing-v2-20260919`.

## Correcciones posteriores incluidas

- Next.js y eslint-config-next 16.3.5; lockfile sincronizado; Node 24.x.
- Menú móvil: foco contenido, contenido cubierto inerte, restitución del foco al cerrar,
  foco en la sección elegida y desbloqueo del scroll al pasar a escritorio.
- Redirecciones permanentes de las seis rutas anteriores. `/informes` lleva a Academy,
  que contiene la biblioteca; `/panel`, retirado, vuelve al inicio.
- Contacto principal enlazado al Instagram público ya existente.
- Favicon SVG basado en el rombo de la cabecera.
- Workflow de validación para `main` y pull requests.

## Verificaciones locales

Lint sin avisos; build y typecheck correctos; npm audit sin vulnerabilidades.
Prueba en navegador: Tab permanece en el menú, selección de Academy mueve el foco a
la sección, Escape devuelve el foco al botón, cambio 390 → 1440 px cierra el menú
y restaura el desplazamiento. Sin errores ni avisos en la consola consultada.

## Pendientes editoriales

News contiene dos demostraciones explícitas. Biblioteca y ruta introductoria aún sin
contenido publicado. Falta completar el enlace al repositorio del juego, revisar el tono
institucional y registrar la confirmación del propietario sobre los retratos. El documento
de auditoría del ZIP es histórico y no sustituye la validación de cambios posteriores.
